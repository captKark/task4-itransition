# Task 4: User Management System

A full-stack user management dashboard built with React (Vite), Express, Node.js, and PostgreSQL.

## Prerequisites
- Node.js (v18+)
- PostgreSQL

## Installation & Setup

### 1. Database Setup
Execute the queries inside `database.sql` to initialize your local table structure.

### 2. Backend Configuration
Navigate to the server directory:
```bash
cd server
npm install